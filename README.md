# 🔍 Reverse Text Search — Gazeta.ru

Проект представляет собой систему обратного текстового поиска по архивным новостям **Gazeta.ru**, использующую сочетание эмбеддингов, векторного поиска и переранжирования через LLM.
Система позволяет искать релевантные статьи по коротким или длинным текстовым запросам, выдавая результаты в порядке смысловой близости.

Основные возможности:

* Создание эмбеддингов для полного корпуса новостей
* Быстрый поиск через **FAISS**
* Улучшение выдачи с помощью LLM-переранжирования
* Пользовательский интерфейс на **Streamlit**
* Полностью контейнеризированный запуск через **Docker**

---

## ⚡ Используемые технологии

* **Gazeta.ru Dataset (HuggingFace)**  
Набор данных новостных текстов из русскоязычного медиапространства, доступный по ссылке:
https://huggingface.co/datasets/IlyaGusev/gazeta  
Используется в качестве корпуса документов для построения индекса и выполнения поиска.


* **Модель intfloat/multilingual-e5-base**  
Многоязычная модель эмбеддингов, предназначенная для задач семантического поиска и retrieval. Применяется для кодирования документов и пользовательских запросов.


* **FAISS (Facebook AI Similarity Search)**  
Библиотека высокопроизводительного поиска ближайших соседей. Используется для построения и хранения векторного индекса, ускоряя семантический поиск по большой коллекции текстов.


* **OpenRouter LLM — Grok 4.1 Fast**  
Внешняя LLM-модель, используемая на этапе переранжирования найденных документов. Позволяет улучшать результаты за счёт глубокой языковой обработки.


* **Streamlit**  
Фреймворк для создания веб-интерфейса. Используется для реализации простого и удобного UI для ввода запросов и отображения результатов.


* **Docker / Docker Compose**  
Используются для контейнеризации проекта, обеспечения воспроизводимой среды, развёртывания и изоляции зависимостей.

---

## 📁 Структура проекта

```
reverse_text_search_gazetaru/
│
├── data/                       # обработанные данные
│
├── embeddings/                 # сохраненные эмбеддинги и метаданные
│
├── index/                      # файловый FAISS-индекс
│
├── images/                     # скриншоты результатов тестов
│
├── src/
│   ├── dataset_loader.py       # загрузка и очистка датасета
│   ├── data_encoder.py         # создание эмбеддингов текстов
│   ├── search_api.py           # основные функции поиска по индексу
│   ├── app.py                  # Streamlit-приложение (UI)
│   ├── index_builder.py        # построение FAISS-индекса
│   └── llm_reranker.py         # переранжирование результатов через LLM
│
├── Dockerfile                  # Docker-образ
├── docker-compose.yml          # сборка и запуск сервиса
├── requirements.txt            # Python-зависимости
├── README.md                   # описание проекта
```

---

## 🧪 Тестовые запросы

### Запрос 1: МЧС прилагает все усилия для тушения лесных пожаров в Западной Сибири
![1 1](images/1%201.png)
![1 2](images/1%202.png)

### Запрос 2: Президент России Владимир Путин провёл встречу с министром МВД Колокольцевым
![2 1](images/2%201.png)
![2 2](images/2%202.png)

### Запрос 3: Британские учёные обнаружили новый вид рыб в Тихом океане
![3 1](images/3%201.png)
![3 2](images/3%202.png)

### Запрос 4: Астронавты НАСА провели выход в открытый космос на МКС
![4 1](images/4%201.png)
![4 2](images/4%202.png)

---

## 🚀 Инструкция по запуску

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/dubrovsky220/reverse_text_search_gazetaru.git
cd reverse_text_search_gazetaru
```

### 2. Создайте файл окружения

Создайте файл `.env` в корне проекта:

```
OPENROUTER_API_KEY=ваш_api_ключ
```

API-ключ можно получить на [https://openrouter.ai/](https://openrouter.ai/)

### 3. Запустите Docker-контейнер

```bash
docker compose up --build
```

После успешного запуска приложение будет доступно по адресу:

```
http://localhost:8501
```

